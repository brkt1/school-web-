export enum ComponentAction {
    default,
    add,
    edit,
    detail
}

export enum ElementType {
    Card,
    Collapse
}

export enum Key {
    // PRIMARY_COLOR, 
    // SECONDARY_COLOR, 
    // THIRD_COLOR, 
    THEME,
    LOGO,
    TITLE,
    TELEPHONE,
    EMAIL,
    YOUTUBE,
    TELEGRAM,
    FACEBOOK,
    LINKEDIN,
    INSTRAGRAM,
    TWITTER,
    LOGIN_IMAGE,
    ADDRESS,
    WHATSAPP
}

export enum CompanySettingsStatus {
    ACTIVE,
    INACTIVE
}

export enum PetitionsStatus {
    Open,
    Close
}

export enum MediasType {
    VIDEO = 100,
    IMAGE = 101,
    AUDIO = 102,
    DOCUMENT = 103
}

export enum GalleryLayoutType {
    Thumbnails = 1,
    Masonry = 2,
    Mosaic = 3,
    Slideshow = 4,
    ImageBrowser = 5,
    BlogStyle=6
}