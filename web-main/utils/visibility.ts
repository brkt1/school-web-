export const isVisible = (visibility?: boolean) =>{
    return visibility ? ' block ' : ' hidden '
  }
  export const isColumnVisible = (visibility: boolean) =>{
    return visibility ? '' : 'hidden'
  }